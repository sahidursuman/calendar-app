<%= bootstrap_form_for(@instructor, :html => {:multipart => true}, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-8" ) do |f| %>
	<%# f.text_field :country_of_origin %>
	<% if @instructor.avatar? %>
		<%= f.form_group do %>
			<%= image_tag(@instructor.avatar_url(:thumb), class: 'img-rounded')  %>
	    <%# f.check_box :remove_avatar %>
		<% end %>
	<% end %>
	<% if @instructor.resume? %>
		<i>resume uploaded</i>
		<%= f.check_box :remove_resume %>
	<% end %>


	<%= f.file_field :avatar %>
	<%= f.hidden_field :avatar_cache %>

	<%= f.file_field :resume %>
	<%= f.hidden_field :resume_cache %>

	<%= f.file_field :credential %>
	<%= f.hidden_field :credential %>

	<%= f.form_group :country, label: {text: "Country"} do %>
	  <%= f.country_select :country_of_origin, {priority_countries: ["US", "CA", "GB"], prompt: 'Select a country'}, class: 'form-control' %>
  <% end %>

	<%= f.text_field :first_language %>
	<%= f.text_field :subject_to_teach %>
	<%= f.text_field :education %>
	<%= f.number_field :price_cents %>
	<%= f.text_field :skype %>
	<%= f.text_area :description, rows: 8 %>
	<%= f.form_group do  %>
	  <%= f.primary "Register" %> 
  <% end %>
<% end %>
