<%= bootstrap_form_for([@instructor, @availability]) do |f| %>
  <% if @availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availability.errors.count, "error") %> prohibited this availability from being saved:</h2>
      <ul>
      <% @availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.datetime_select :start_time, discard_year: true, minute_step: 30, ampm: true %>

  <%= f.datetime_select :end_time, discard_year: true, minute_step: 30, ampm: true %>
  
	<%= f.form_group do  %>
	  <%= f.primary "Create Availability" %> 
  <% end %>
<% end %>


